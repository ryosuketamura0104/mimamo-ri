services:
  postgres:
    image: postgres:16-alpine
    container_name: mimamori-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-mimamori}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mimamori}
      POSTGRES_DB: ${POSTGRES_DB:-mimamori}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-mimamori} -d ${POSTGRES_DB:-mimamori}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
